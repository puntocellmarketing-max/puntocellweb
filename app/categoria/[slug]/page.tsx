import Link from "next/link";
import { ChevronRight } from "lucide-react";
import { notFound } from "next/navigation";
import ProductGrid from "@/app/components/ProductGrid";
import { getPublicCategories,getPublicProducts } from "@/lib/ecommerce";
export const dynamic="force-dynamic";
export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const[categories,products]=await Promise.all([getPublicCategories(),getPublicProducts({categorySlug:slug,limit:100})]);const category=categories.find(item=>item.slug===slug);if(!category)notFound();return <main className="mx-auto max-w-7xl px-4 py-8 lg:px-6"><nav className="flex items-center gap-2 text-xs font-semibold text-slate-500"><Link href="/">Inicio</Link><ChevronRight className="h-3 w-3"/><span className="text-slate-900">{category.name}</span></nav><div className="mt-7 rounded-3xl bg-slate-950 px-6 py-10 text-white sm:px-10"><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-400">Catálogo PuntoCell</p><h1 className="mt-3 text-3xl font-black sm:text-4xl">{category.name}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">{category.description||`Explorá productos de ${category.name.toLowerCase()} y consultá disponibilidad.`}</p></div><div className="mt-8 flex items-center justify-between"><p className="text-sm font-semibold text-slate-500">{products.length} productos encontrados</p></div><div className="mt-5">{products.length?<ProductGrid products={products}/>:<div className="grid min-h-72 place-items-center rounded-2xl border bg-white text-center"><div><p className="font-black">Todavía no hay productos publicados</p><p className="mt-2 text-sm text-slate-500">Volvé pronto o consultanos por WhatsApp.</p></div></div>}</div></main>}

