"use client";
import { Edit3, ImageIcon, Loader2, Plus, Trash2 } from "lucide-react";
import { useCallback,useEffect,useState } from "react";
import AdminImageUpload from "../components/AdminImageUpload";
type Banner={id:number;title:string;subtitle:string|null;image_url:string|null;mobile_image_url:string|null;button_label:string|null;button_url:string|null;theme:string;position:string;starts_at:string|null;ends_at:string|null;active:number;sort_order:number};
const empty={title:"",subtitle:"",imageUrl:"",mobileImageUrl:"",buttonLabel:"Ver productos",buttonUrl:"#productos",theme:"blue",position:"HOME_MAIN",startsAt:"",endsAt:"",active:true,sortOrder:"0"}; const field="h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-blue-500";
function localDate(value:string|null){return value?new Date(value).toISOString().slice(0,16):"";}
export default function BannersAdminClient(){const[items,setItems]=useState<Banner[]>([]);const[form,setForm]=useState(empty);const[editId,setEditId]=useState<number|null>(null);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);try{const r=await fetch("/api/admin/ecommerce/banners",{cache:"no-store"});const j=await r.json();if(!r.ok||!j.ok)throw new Error(j.error);setItems(j.banners);}catch(e:any){setError(e?.message||"No se pudieron cargar los banners.");}finally{setLoading(false);}},[]);useEffect(()=>{load();},[load]);function reset(){setForm(empty);setEditId(null);setError("");}async function save(e:React.FormEvent){e.preventDefault();setSaving(true);setError("");try{const r=await fetch(editId?`/api/admin/ecommerce/banners/${editId}`:"/api/admin/ecommerce/banners",{method:editId?"PUT":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const j=await r.json();if(!r.ok||!j.ok)throw new Error(j.error);reset();await load();}catch(e:any){setError(e?.message||"No se pudo guardar.");}finally{setSaving(false);}}async function hide(item:Banner){if(!confirm(`¿Ocultar el banner “${item.title}”?`))return;await fetch(`/api/admin/ecommerce/banners/${item.id}`,{method:"DELETE"});load();}
return <div><div className="flex items-end justify-between"><div><p className="text-xs font-extrabold uppercase tracking-[.18em] text-blue-600">Página principal</p><h1 className="mt-2 text-3xl font-black">Banners dinámicos</h1><p className="mt-2 text-sm text-slate-500">Programá promociones sin modificar el código.</p></div><button onClick={reset} className="hidden h-11 items-center gap-2 rounded-xl border bg-white px-4 text-sm font-bold sm:flex"><Plus className="h-4 w-4"/>Nuevo</button></div><div className="mt-7 grid items-start gap-6 xl:grid-cols-[420px_1fr]"><form onSubmit={save} className="rounded-2xl border bg-white p-5 shadow-sm xl:sticky xl:top-24"><h2 className="font-black">{editId?"Editar banner":"Nuevo banner"}</h2><div className="mt-5 grid gap-4"><AdminImageUpload value={form.imageUrl} onChange={v=>setForm({...form,imageUrl:v})} folder="banners"/><label className="grid gap-1.5 text-xs font-bold">Título *<input required className={field} value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label className="grid gap-1.5 text-xs font-bold">Texto<textarea className={`${field} min-h-20 py-3`} value={form.subtitle} onChange={e=>setForm({...form,subtitle:e.target.value})}/></label><div className="grid grid-cols-2 gap-3"><label className="grid gap-1.5 text-xs font-bold">Botón<input className={field} value={form.buttonLabel} onChange={e=>setForm({...form,buttonLabel:e.target.value})}/></label><label className="grid gap-1.5 text-xs font-bold">Enlace<input className={field} value={form.buttonUrl} onChange={e=>setForm({...form,buttonUrl:e.target.value})}/></label></div><div className="grid grid-cols-2 gap-3"><label className="grid gap-1.5 text-xs font-bold">Desde<input type="datetime-local" className={field} value={form.startsAt} onChange={e=>setForm({...form,startsAt:e.target.value})}/></label><label className="grid gap-1.5 text-xs font-bold">Hasta<input type="datetime-local" className={field} value={form.endsAt} onChange={e=>setForm({...form,endsAt:e.target.value})}/></label></div><div className="grid grid-cols-2 gap-3"><label className="grid gap-1.5 text-xs font-bold">Color<select className={field} value={form.theme} onChange={e=>setForm({...form,theme:e.target.value})}><option value="blue">Azul</option><option value="navy">Azul oscuro</option><option value="violet">Violeta</option><option value="emerald">Verde</option><option value="orange">Naranja</option></select></label><label className="grid gap-1.5 text-xs font-bold">Orden<input type="number" className={field} value={form.sortOrder} onChange={e=>setForm({...form,sortOrder:e.target.value})}/></label></div><label className="flex gap-2 text-sm font-semibold"><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/>Banner publicado</label>{error&&<p className="rounded-xl bg-rose-50 p-3 text-sm font-semibold text-rose-700">{error}</p>}<button disabled={saving} className="flex h-11 items-center justify-center gap-2 rounded-xl bg-blue-600 font-bold text-white">{saving&&<Loader2 className="h-4 w-4 animate-spin"/>}Guardar banner</button></div></form><section className="grid content-start gap-4 sm:grid-cols-2">{loading?<div className="col-span-full grid min-h-64 place-items-center"><Loader2 className="animate-spin text-blue-600"/></div>:!items.length?<div className="col-span-full grid min-h-64 place-items-center rounded-2xl border bg-white text-center"><div><ImageIcon className="mx-auto text-slate-300"/><p className="mt-3 font-bold">Sin banners cargados</p></div></div>:items.map(item=><article key={item.id} className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className={`grid h-40 place-items-center bg-gradient-to-br ${item.theme==='emerald'?'from-emerald-600 to-teal-400':item.theme==='violet'?'from-violet-700 to-fuchsia-500':item.theme==='orange'?'from-orange-600 to-amber-400':item.theme==='navy'?'from-slate-950 to-blue-800':'from-blue-700 to-cyan-500'}`}>{item.image_url?<img src={item.image_url} alt="" className="h-full w-full object-cover"/>:<ImageIcon className="h-12 w-12 text-white/60"/>}</div><div className="p-4"><div className="flex items-center gap-2"><span className={`rounded-full px-2 py-1 text-[10px] font-black uppercase ${item.active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500'}`}>{item.active?'Publicado':'Oculto'}</span></div><h3 className="mt-3 font-black">{item.title}</h3><p className="mt-1 line-clamp-2 text-xs leading-5 text-slate-500">{item.subtitle}</p><div className="mt-4 flex justify-end gap-2"><button onClick={()=>{setEditId(item.id);setForm({title:item.title,subtitle:item.subtitle||'',imageUrl:item.image_url||'',mobileImageUrl:item.mobile_image_url||'',buttonLabel:item.button_label||'',buttonUrl:item.button_url||'',theme:item.theme,position:item.position,startsAt:localDate(item.starts_at),endsAt:localDate(item.ends_at),active:Boolean(item.active),sortOrder:String(item.sort_order)});scrollTo({top:0,behavior:'smooth'});}} className="rounded-xl border p-2.5"><Edit3 className="h-4 w-4"/></button><button onClick={()=>hide(item)} className="rounded-xl border p-2.5 text-rose-600"><Trash2 className="h-4 w-4"/></button></div></div></article>)}</section></div></div>}

